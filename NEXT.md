# Next

1. Run `bash scripts/pod_setup.sh` on the target pod.
2. Decide whether the next spend goes to `legs/2026-04-12_midnight_iii_clean` or a fresh child leg.
3. If starting a new idea, create it with `bash scripts/new_leg.sh <name>`.
4. Record every test in the leg files, not the shell history.
