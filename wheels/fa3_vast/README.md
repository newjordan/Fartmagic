Repo-local emergency FA3 wheel fallback.

Use this only as an explicit artifact install from `scripts/Im_sorry_pod_setup.sh`.
Do not replace this with a generic `pip install flash-attn` or `pip install flash-attn-3` path.
